import {useEffect, useRef} from "react";
import * as React from "react";
import {FileUpload, Overlay, OverlayButtonContainer, UploadLabel} from "_renderer/components/RemarkableModel/styles";
import background from "_renderer/static/background.png";

export function Settings() {

    return (
        <div>
            Settings
        </div>
    )
}

export default Settings
